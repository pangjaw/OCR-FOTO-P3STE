# 2026-07-13 Daily Log

## Summary
Major architectural refactor: Shifted from flat `asset_type/detail` folder structure to hierarchical `station/asset_type/detail` structure across the entire pipeline. This resolves the "ZP 41B BOO" vs "ZP 41 BOO" ambiguity and enables correct photo-to-Funcloc mapping in merge step.

## Changes Made

### 1. `export_pdf_foto.py` (Export)
- **AssetRow dataclass**: Added `station` field
- **`detect_asset_type()`**: Extended to support new asset types (CATU_DAYA, PINTU_PERLINTASAN, TELEKOMUNIKASI, PERSINYALAN_ELEKTRIK)
- **`extract_asset_rows()`**: New signature `(page, sap_mapping)` — loads `sap_station_mapping.json` to resolve `Functional Loc.` → `Station`
- **`asset_output_dir()`**: Changed signature to `(root, station, asset_type, detail)` → outputs to `root/station/asset_type/detail/`
- **`export_pdf()`**: Threads `sap_mapping` through; passes `station` to `asset_output_dir`

### 2. `extract_pdf_dates.py` (Extract Dates)
- Updated to import and use new `asset_output_dir(root, station, asset_type, detail)` signature

### 3. `scheduler.py` (Scheduling)
- **New**: `load_sap_mapping()` — loads `sap_station_mapping.json`
- **`build_schedule()`**: Accepts `sap_mapping`; passes to `extract_asset_rows()`
- **`get_waktu()`**: Extended defaults for new asset types (CATU_DAYA=45, PINTU_PERLINTASAN=45, TELEKOM=60-120, PERSINYALAN=420)

### 4. `edit_timemark_ide1.py` (Edit Timemark)
- **`_collect_folder_consensus()`**: Updated key tuple from `(asset_type, detail)` → `(station, asset_type, detail)` to match new 4-level folder depth
- **Main loop**: Path parsing now handles 3 folder levels (`parts[-3]`, `parts[-2]`, `parts[-1]`)

### 5. `merge_pdf_foto.py` (Merge)
- Verified `extract_asset_rows` import uses updated signature from `export_pdf_foto.py` — maintains parity for photo lookup at `04_photos_edited/Tim_{n}/{station}/{asset_type}/{detail}/`

### 6. `AGENTS.md` (Documentation)
- Updated folder structure diagram to show 4-level hierarchy
- Updated all script references: inputs/outputs, function signatures, key changes
- Added `station` parameter throughout

### 7. `Dashboard.md` (Status Tracker)
- Updated folder structure diagram
- Marked structural migration complete

## Files Modified
- `export_pdf_foto.py`
- `extract_pdf_dates.py`
- `scheduler.py`
- `edit_timemark_ide1.py`
- `AGENTS.md`
- `Dashboard.md`

## Verification
- All 6 core Python scripts compile without syntax errors (`python -m py_compile`)
- Architecture decision recorded: station-based hierarchy resolves duplicate detail names across stations

## Next Steps
- [ ] End-to-end pipeline test on sample PDF (export → extract dates → schedule → edit → merge)
- [ ] Verify photo-to-Funcloc alignment in merged PDF output
- [ ] Test with `--schedule` flag for Tim_1/Tim_2 subfolder routing
