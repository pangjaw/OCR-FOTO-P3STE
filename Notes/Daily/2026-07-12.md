# 📅 Daily Note - 2026-07-12

> [!summary] **Ringkasan**
> Fix browser freeze di `app.py` — limit file listing API ke 100 file + metadata total/truncated. Frontend hanya render 100 `<li>` max.

---

## 🔧 Fix: Browser Freeze saat Load 2000+ File

**Masalah:** Browser freeze saat `app.py` dijalankan karena folder `01_pdf_source` dan `02_pdf_target` berisi total ~359 PDF (dan subfolder lainnya). API `/api/files` mengembalikan semua file sekaligus, dan frontend merender semua ke DOM sebagai `<li>` element.

**Solusi:**
1. **Backend (`app.py`):** `get_pdf_list()` sekarang return `{"files": [...], "total": N, "truncated": true/false}` dengan limit default 100 file.
2. **Frontend (`templates/index.html`):** 
   - **Simplify File Listing:** Ganti daftar file per folder menjadi tampilan "TOTAL FILE = N" saja untuk menghilangkan freeze dari render banyak DOM element.
   - **EventSource reconnect loop fix:** Cegah multiple SSE connections, cleanup `beforeunload`, delay reconnect 3s.

**File terubah:**
- `app.py` — `get_pdf_list()` + `api_files()`
- `templates/index.html` — Simplified count display + EventSource fix
- `Dashboard.md` — status updated

---

## 🎯 Next Actions
- [ ] Test `app.py` dengan folder berisi banyak file untuk verifikasi freeze tidak terjadi lagi.
- [ ] (Opsional) Tambah pagination atau virtual scrolling jika user perlu melihat semua file.
