# 📅 Daily Log - 2026-07-11

## Completed
- **edit_timemark_ide1.py** — Add `--schedule` arg + `iso_to_timemark()` format + output ke `Export_Foto/Tim_{n}/...`
- **merge_pdf_foto.py** — Add `--schedule` arg for Tim folder photo lookup
- **scheduler.py** — Created, generates `schedule.json` from PDF scan + asset_waktu_mapping
- **Folder Restructure** — full rename:
  - `input_pdf` → `01_pdf_source`
  - `pdf_imo` → `02_pdf_target`
  - `output_pdf_foto` → `03_photos_export`
  - `Export_Foto` → `04_photos_edited`
  - `hasil_gabung` → `05_pdf_merged`
  - Hapus folder mati: `preview/`, `templates/`, `tmp/`, `input_foto/`, `output_foto/`, `Export_Foto/` (root)
  - Update path defaults di 6 script + app.py + hapus ui_config.json lama
  - Old folders (`input_pdf/`, `pdf_imo/`, etc) masih ada — hapus manual setelah verifikasi

## Pipeline Baru (Final)
1. `export_pdf_foto.py` → ekstrak foto dari PDF
2. `extract_pdf_dates.py` → ekstrak tanggal
3. `scheduler.py --pdf-dir pdf_imo --photos-dir output_pdf_foto --output schedule.json`
4. `edit_timemark_ide1.py --input output_pdf_foto --schedule schedule.json`
5. `merge_pdf_foto.py --input pdf_imo --photos output_pdf_foto/Export_Foto --schedule schedule.json`

## Next
- Testing full pipeline dengan schedule
