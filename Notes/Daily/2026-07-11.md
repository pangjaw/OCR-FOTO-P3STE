# 📅 Daily Log — 2026-07-11

> ← [[Notes/Daily/2026-07-10|10 Jul]] | [[Dashboard]] | [[Notes/Daily/2026-07-12|12 Jul →]]


> [!summary] **Ringkasan Hari Ini**
> - **Bug Fix**: Folder consensus (`_folder_key_from_path`) sekarang mendukung input folder aset tunggal (mis. `03_photos_export/AXC/ZP 42B BOO/`) selain full tree (`03_photos_export/AXC/`).
> - **Root Cause**: Saat input adalah folder aset spesifik, `relative_to(input_dir)` hanya mengembalikan nama file (`0.jpg`), bukan `detail/0.jpg`. Fungsi diperbaiki untuk mengambil `asset_type` dari `input_dir.parent.name` dan `detail` dari `input_dir.name` saat `len(parts) == 1`.
> - **Test**: Pipeline penuh dijalankan pada folder `ZP 42B BOO` (3 foto) → 3/3 sukses, consenso gy1=185 terbentuk. Lalu full AXC (402 foto) → 402/402 sukses.

---

## 🛠️ Perubahan Kode

### `edit_timemark_ide1.py`
- **Function `_folder_key_from_path`** (line ~374): Tambahkan handling untuk `len(parts) == 1` (input folder aset langsung). Ambil `asset_type` dari `input_dir.parent.name`, `detail` dari `input_dir.name`.
- **Call sites**: `_collect_folder_consensus` (line ~403) dan loop `main()` (line ~493) sekarang melewatkan argumen `input_dir`.

---

## ✅ Verifikasi

| Skenario | Foto | Hasil |
|----------|------|-------|
| Single asset folder (`ZP 42B BOO`) | 3 | ✅ 3/3 sukses, consensus gy1=185 |
| Full tree (`AXC/`) | 402 | ✅ 402/402 sukses |

**Stage usage**: Semua file di `ZP 42B BOO` menggunakan **Stage 1c (Guide original/consensus)** — tidak ada yang fallback ke Stage 4 (skip). Box ditempatkan sejajar dengan batas atas Red Guide (`y1 = guide_y1`), tinggi box 16px, padding mask 2px → visual rapi, tidak menutupi alamat.

---

## 📝 Catatan Teknis

- Noise oranye di belakang Red Guide (yg menyebabkan `find_red_guide` gagal pada 50.jpg) kini diatasi via **folder consensus**: 0.jpg & 100.jpg konsisten di gy1=185 → diterapkan ke 50.jpg.
- Logika fallback di `locate_date_box`: `y_override` → `find_red_guide` (Stage 1c) → `folder_consensus` → `get_text_box` (default).
- `diffuse_fill_region` 120 iterasi (diffusion inpainting) + narrow mask 5px ke atas → erase bersih tanpa blur lebar.

---

## 🚀 Next Steps

- [ ] Jalankan full pipeline batch 2026 (Step 1→5 via Web UI atau CLI) untuk memastikan tidak ada regresi.
- [ ] Update `AGENTS.md` jika ada perubahan arsitektur signifikan.
- [ ] Commit & push ke GitHub.
