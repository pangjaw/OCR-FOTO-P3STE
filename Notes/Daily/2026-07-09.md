# 📅 Daily Log - 2026-07-09

> ← [[Notes/Daily/2026-07-08|8 Jul]] | [[Dashboard]] | [[Notes/Daily/2026-07-10|10 Jul →]]


## 📋 Target Hari Ini
- [x] Implementasi Pola OCR Lokal (Pola ke-2) pada `edit_timemark.py` untuk mencari baris teks tanggal secara langsung menggunakan koordinat kata yang dideteksi oleh Tesseract OCR.
- [x] Tambahkan filter kata kunci hari (Senin-Minggu) dan bulan (Januari-Desember, Jan-Des) untuk meningkatkan akurasi pendeteksian.
- [x] Perbaiki lebar date box agar selalu konstan (tidak menyusut saat Tesseract hanya mendeteksi sebagian teks), memastikan seluruh area tanggal termasuk "AM/PM" terhapus sempurna dan ukuran font tetap proporsional.
- [x] Perluas pencarian kata kunci OCR lokal mencakup: singkatan bulan tambahan ("AGU"), tahun ("2024", "2025", "2026"), serta penanda waktu ("AM", "PM", "WIB", "WITA", "WIT") agar foto tanpa nama hari tetap terdeteksi 100% akurat.
- [x] Terapkan multi-threshold binarisasi (180, 160, 200) dalam loop deteksi untuk toleransi noise OCR yang tinggi pada teks yang agak kabur.
- [x] Urutkan prioritas pencarian kata kunci OCR: Tahap 1 mendeteksi bulan, tahun, dan penanda waktu (paling spesifik), kemudian Tahap 2 mencari nama hari sebagai fallback (belakangan) guna menghindari salah deteksi (false-positives) pada baris teks lain.
- [x] Jalankan script `edit_timemark.py` pada seluruh folder input `output_pdf_foto` (168/168 foto diproses sukses).
- [x] Jalankan pengujian OCR validator pada seluruh berkas keluaran `output_pdf_foto/Export_Foto` (168/168 file clean, 0 leftovers).
- [x] Buat script baru `edit_timemark_ide1.py` yang menerapkan Ide 1 (rata-rata Y-center dari kata kunci terdeteksi) untuk akurasi penempatan box tanggal baru yang 100% konsisten.
- [x] Lakukan batch process menggunakan `edit_timemark_ide1.py` pada seluruh 168 berkas foto dan verifikasi kelulusannya.

## ✍️ Catatan Aktivitas
- Menambahkan fungsi `find_date_box_by_ocr` pada `edit_timemark.py` yang menggunakan `pytesseract.image_to_data` pada crop binarized daerah watermark kiri bawah.
- Mengintegrasikan deteksi OCR lokal sebagai prioritas pertama di `locate_date_box` sebelum fallback ke red guide dan koordinat default.
- Mengubah `find_date_box_by_ocr` agar mengembalikan lebar area box standar (`x1` dan `x2` konstan) untuk mencegah box menyusut akibat kegagalan deteksi parsial.
- Memperluas set kata kunci pencarian OCR ke hari, bulan (termasuk "AGU"), tahun (2024-2026), dan penanda waktu (AM/PM/WIB/dst) serta loop multi-threshold.
- Mengubah alur kerja pencarian OCR lokal menjadi 2-pass: Pass 1 memindai bulan/tahun/penanda waktu, Pass 2 memindai nama hari sebagai cadangan.
- Memproses ulang seluruh 168/168 foto baru di `output_pdf_foto/` ke `output_pdf_foto/Export_Foto/` dengan tanggal `Minggu, Jul 08 2024`.
- Menjalankan validator [validate_ocr_results.py](file:///c:/Users/dikarm/Documents/Server/OCR-FOTO-P3STE/scratch/validate_ocr_results.py), menghasilkan kelulusan mutlak: **168/168 berkas sukses bersih 100%**.
- Membuat berkas baru `edit_timemark_ide1.py` yang menerapkan logika penentuan posisi Y berbasis rata-rata Y-center dari kata kunci tanggal yang dideteksi oleh Tesseract.
- Menjalankan batch process dengan `edit_timemark_ide1.py` pada 168 foto asli. Hasilnya **167/168 berkas bersih sempurna**, dengan 1 berkas `SINYAL\KELUAR DAN LANGSIR JL12A BOO\100.jpg` memicu alarm false positive akibat Tesseract salah membaca struktur tiang besi penopang listrik (gantry) di latar belakang sebagai teks "2025" (gambar itu sendiri sudah ter-edit dengan 100% rapi).
- Melakukan investigasi mendalam terhadap `LANGSIR L20 BOO/50.jpg` (karena tahun "2026" tergabung menjadi `"832006"` di OCR). Memperluas deteksi tahun ke rentang 2001–2999 dengan regex `20\d{2}`.
- Melakukan investigasi terhadap `JL32A BOO/50.jpg` (karena false positive kawat pagar dibaca `"May"`). Menghapus singkatan Inggris (`MAY`, `AUG`, `OCT`, `DEC`) dari kamus bulan untuk menghindari kesalahan pencocokan pada noise latar belakang.
- Melakukan investigasi terhadap `ZP 21A BOO/100.jpg` (karena teks tanggal fisik terpotong garis diagonal peron semen stasiun). Mengintegrasikan metode binarisasi adaptif lokal **Sharpening + adaptive thresholding (BoxBlur)** sebagai pass ke-4 di loop OCR. Teks tanggal berhasil dibaca dan diposisikan 100% akurat secara otomatis, menyelesaikan kebocoran watermark.
- Menjalankan batch process akhir pada seluruh 168 berkas foto dengan hasil kelulusan visual 100% rapi dan bersih.

## 💡 Ide / Catatan Tambahan
- Prioritisasi kata kunci bulan/tahun/AM-PM di atas nama hari meniadakan risiko salah deteksi akibat teks luar (seperti papan jalan atau teks box fisik) yang terbaca sebagai nama hari.
- Logika rata-rata Y-center (Ide 1) terbukti sangat kokoh terhadap noise perluasan vertikal bounding box kata (misalnya akibat bayangan/kerikil menyatu dengan teks asli). Kotak watermark baru selalu tergambar simetris dan presisi di tengah garis baseline tanggal lama.
- Binarisasi adaptif lokal sangat ampuh memisahkan teks putih dari latar belakang yang warnanya/kecerahannya sangat mirip (seperti semen abu-abu atau peron diagonal) dengan cara mengekstrak kontras lokal. Ini sangat memperkuat ketahanan OCR lokal pada kondisi tersulit.

## ⚙️ Sesi Siang: Eksperimen OCR Engine Alternatif

### PaddleOCR (gagal)
- Install `paddlepaddle` (v3.3.1) + `paddleocr` (v3.7) di `.venv_paddle`
- **Bug:** oneDNN `ConvertPirAttribute2RuntimeAttribute not support` di Windows
- Fix: `enable_mkldnn=False` — masih error
- **Skip:** terlalu berat & lama. Gak cocok CPU-only.

### EasyOCR (coba preprocessing)
- Install `torch` + `easyocr` di `.venv_paddle` yang sama (bentrok? aman, PyTorch beda env dgn Paddle)
- Hasil OCR tanpa preprocessing: mendeteksi teks alamat (`Jlmayor Oking`), bukan timemark tanggal
- Hasil dengan preprocessing (grayscale + adaptive threshold + upscale): jadi garbage (`�al�yaroing`)
- **Kesimpulan:** Tesseract tetap paling cocok karena `--psm 7` bisa fokus ke 1 baris. EasyOCR gak punya setara PSM mode.

### Batch Final (Tesseract)
- `edit_timemark_ide1.py` — recursive scan semua subfolder
- Input: `output_pdf_foto` (exclude Export_Foto otomatis)
- Output: `output_proses_all`
- **Hasil: 168/168 ✅ sukses bersih**

## ⚙️ Sesi Sore: Stage 2 Fallback — Deteksi Alamat

- **Masalah:** JL32A 50 punya tanggal noise parah, Stage 1 gagal. Butuh fallback yg gak peduliin tanggal, cari alamat lalu taruh date baru di atasnya.
- **Konsep:** Stage 1 cari keyword tanggal (bulan/tahun/hari). Gagal → Stage 2: crop area kiri-bawah (1%-38% x 55%-88%), threshold 150, PSM 4, deteksi baris teks. Ambil Y teratas → taruh date box 25px di atasnya.
- **Prioritas left < 10:** address yg rata kiri (Stasiun, Jl.) diprioritaskan di atas noise tengah.
- **Thresh 150 + PSM 4** jadi sweet spot: noise rendah, Stasiun/Bogor kebaca.
- **Status:** Batch ulang 168/168 ✅

- **Evolusi Ide (Dynamic Gap untuk Fallback):**
  - Masalah: Deteksi Y alamat di fallback tidak konsisten karena kualitas OCR (terkadang mendeteksi baris 1 `Stasiun` di Y=194, terkadang baris 2 `Tengah` di Y=221, atau baris 3 `Bogor` di Y=235).
  - Solusi: Menghapus filter `count >= 2` dan menerapkan pencarian baris paling atas yang rata kiri (`left < 30px`).
  - Untuk menyesuaikan posisi penempatan agar selalu menutupi tanggal lama (Y=182-185) secara presisi, kita menerapkan **Dynamic Gap** berbasis rasio tinggi gambar (`y_top / h`):
    - Jika `y_top / h < 0.70` (baris 1 terdeteksi): `gap = 0px` (untuk gambar 300px) agar kotak dipasang di Y=181-194 (mepet di atas huruf alamat dan menutup tanggal lama dengan sangat bersih).
    - Jika `0.70 <= y_top / h < 0.76` (baris 2 terdeteksi): `gap = 25px` agar kotak dipasang di Y=183-196.
    - Jika `y_top / h >= 0.76` (baris 3 terdeteksi): `gap = 40px` agar kotak dipasang di Y=182-195.

- **Optimasi Stage 1 (Threshold 215):**
  - Masalah: Pada foto ZP 92 50, Stage 1 gagal mendeteksi tanggal lama meskipun teksnya secara visual terbaca. Hal ini disebabkan karena background kerikil/pasir yang terang ikut masuk di threshold rendah (160/180/200), membuat confidence OCR jatuh di bawah 30.
  - Solusi: Menambahkan threshold tinggi `215` pada loop Stage 1 untuk menolak noise latar belakang abu-abu (menyisakan teks putih solid). Tesseract berhasil mendeteksi `Jan` dengan confidence 95% di threshold tinggi ini, sehingga program menggunakan **Stage 1** (paling presisi) alih-alih fallback.

- **Bugfix Erase Mask (Masking Tanggal Lama):**
  - Temuan: Pada foto ZP 62B 50, meskipun date box digambar di posisi yang benar (Y=181-194), tanggal lama di atasnya (`Jan 10, 2026 AM`) masih terlihat dan tidak terhapus.
  - Masalah: Bug logika pada pembuatan mask di `process_image`. Nilai mask `255` hanya digambar pada koordinat `rounded_rectangle` setinggi `shape_box` saja. Area tambahan 14px ke atas (`y1p = sy1 - pad - 14`) yang dikirim ke `diffuse_fill_region` memiliki nilai mask `0`, sehingga piksel tanggal lama di area tersebut tidak pernah diganti/dihapus lewat difusi.
  - Solusi: Mengubah penggambaran mask agar dimulai dari Y=0 di crop area (`mask_draw.rounded_rectangle((rx1, 0, rx2, ry2))`), sehingga area perluasan 14px ke atas ikut terhapus bersih secara difusi. Hasilnya tanggal lama hilang total dan hanya menyisakan date box baru.

- **Optimasi Blur Mepet Textbox + Tinggi Box 16px (Final):**
  - Temuan: User meminta area blur disamakan dengan tinggi textbox saja tanpa adaasan perluasan mask ke atas agar hasil difusi benar-benar sempit dan rapi.
  - Masalah: Jika tinggi date box baru terlalu kecil (13px), sisa karakter tanggal lama yang tingginya 14px akan menyembul keluar di atas/bawah textbox.
  - Solusi: 
    1. Menaikkan tinggi date box baru `box_h` dari `int(h * 0.046)` (13px) menjadi `int(h * 0.053)` (16px untuk h=300).
    2. Menghapus perluasan mask ke atas (`y1p = max(0, sy1 - pad)`). Area blur kini disamakan persis dengan tinggi `shape_box` baru + padding 3px saja.
    3. Karena tinggi box baru (16px) sekarang lebih besar dari tanggal lama (14px), tanggal lama berada sepenuhnya di dalam area hapus textbox. Hasilnya tanggal lama terhapus bersih total 100% tanpa menyisakan blur yang lebar di latar belakang.
- **Folder-Level Cross-File Reference (Konsensus Cerdas):**
  - Temuan: Inkonsistensi Y terjadi dalam satu folder karena file yang OCR-nya berhasil menggunakan OCR lokal, sedangkan file yang OCR-nya gagal menggunakan Red Guide / fallback default.
  - Masalah: File dokumentasi dalam satu folder aset (`0.jpg`, `50.jpg`, `100.jpg`) secara fisik memiliki layout Y tanggal dan alamat yang identik, namun diproses dengan koordinat berbeda akibat variasi keberhasilan OCR lokal.
  - Solusi:
    1. Membuat modular helper `detect_date_y_center` (Stage 1) dan `detect_address_y_top` (Stage 2) untuk pemindaian individual.
    2. Menambahkan tahap Pre-scan Folder di fungsi `main` untuk mengelompokkan images dan memindai semua file di folder tersebut.
    3. Menerapkan aturan voting konsensus: Jika $\ge 2$ file di folder tersebut berhasil mendeteksi Y tanggal lama (Stage 1), maka rata-rata Y tanggal tersebut digunakan secara mutlak oleh semua file di folder tersebut (menyelesaikan masalah file OCR blank/gagal).
    4. Jika tanggal tidak terdeteksi namun $\ge 2$ file memiliki Y alamat yang konsisten (selisih < 10px), gunakan Y alamat konsensus tersebut sebagai fallback.
    5. Prioritas Red Guide (`cached_guide`) dinaikkan di atas OCR lokal di `locate_date_box` agar folder yang memiliki Red Guide (seperti AXC, Sinyal) diproses dengan 100% konsisten menggunakan koordinat Red Guide, sedangkan folder tanpa Red Guide (Wesel) diproses dengan voting konsensus OCR.
    6. Hasil: Rerun batch sukses memproses seluruh 168 gambar dengan 100% konsisten per folder tanpa log fallback tunggal pun.

- **Folder-Level Red Guide Voting (Penyaringan Red Guide Palsu):**
  - Temuan: Pada folder `KELUAR DAN LANGSIR JL52 BOO`, file `50.jpg` diproses dengan posisi date box terlalu tinggi sehingga tanggal lama `Jan 09, 2026 AM` menyembul keluar di bawah kotak baru.
  - Masalah: Terjadi **Red Guide Palsu (False Positive)** lokal pada gambar `50.jpg`. Program mendeteksi garis merah di `Y=185` (kemungkinan karena kabel/objek latar belakang berwarna merah), padahal Red Guide asli dari GPS Map Camera berada di `Y=199` (seperti yang terdeteksi di `0.jpg` dan `100.jpg`). Karena program langsung menggunakan Red Guide lokal, koordinat date box pada `50.jpg` melompat terlalu tinggi ke atas.
  - Solusi:
    1. Membuat **Red Guide Consensus** pada pra-pemindaian folder: Mengumpulkan semua Red Guide dari semua file di folder tersebut, lalu mencari mayoritas koordinat `Y1` yang berjarak dekat (selisih < 10px).
    2. Modus Y1 mayoritas disepakati sebagai Red Guide konsensus folder (pada JL52 disepakati Y1=199).
    3. Di `locate_date_box`, `cached_guide` (konsensus folder) diprioritaskan di atas deteksi lokal (`cached_guide or find_red_guide(arr)`).
    4. Hasil: `50.jpg` dipaksa patuh pada Y1=199, menyejajarkan posisinya secara presisi dengan `0.jpg` dan `100.jpg`, sehingga tanggal lama terhapus bersih total 100%.

- **Penyelarasan Batas Atas Red Guide (y1 = guide_y1) & Box Tinggi 13px (Final):**
  - Temuan: Pada hasil visual awal JL52 `50.jpg` dengan Y1=199, textbox baru ternyata masih memotong sedikit bagian atas huruf kapital (`J` dan `M`) pada alamat di bawahnya.
  - Penyebab: Rumus Template 2 sebelumnya meluncurkan batas atas textbox `y1` sejauh 9px di atas Red Guide (`guide_y1 - 9`), sehingga textbox setinggi 16px menjadi terlalu tebal ke bawah dan menabrak alamat.
  - Solusi:
    1. Mengatur batas atas textbox `y1 = guide_y1` (sejajar sempurna dengan ujung atas Red Guide asli GPS Map Camera).
    2. Mengembalikan tinggi textbox ke standar aman yaitu **13px** (`int(h * 0.046)`) untuk semua stage (Stage 1, Stage 2 fallback, dan Red Guide) untuk mencegah tabrakan dengan alamat.
    3. Mempersempit padding mask `pad` dari 3px ke **2px** untuk memberikan toleransi aman tambahan bagi huruf-huruf alamat di bawahnya.
    4. Hasil: Rerun Batch final 168 foto sukses dengan penempatan date box yang sejajar rapi di atas alamat dan tanggal lama terhapus bersih total 100%.

- **Optimasi Urutan Prioritas Stage (Tukar Stage):**
  - Pembaruan: Urutan prioritas stage ditukar agar data teks fisik (OCR) didahulukan di atas visual guide (Red Guide). Hal ini dilakukan untuk mengabaikan Red Guide Palsu (misalnya tiang/rel berwarna merah di latar belakang foto yang tersambung vertikal dengan oren Red Guide asli seperti pada `ZP 22B 100.jpg` dan `ZP 21A 100.jpg`).
   - Urutan Prioritas Final:
     1. **STAGE 1 (Tanggal Lokal):** OCR mandiri per file untuk mendeteksi Y-center tanggal lama pada gambar itu sendiri.
     2. **STAGE 2 (Alamat Konsensus):** Rata-rata Y-top alamat tingkat folder (OCR) dengan gap dinamis.
     3. **STAGE 3 (Red Guide Lokal):** Koordinat ujung atas Red Guide lokal per file (`y1 = guide_y1`).
     4. **STAGE 4 (Dilewati/Skip):** Jika ketiganya gagal, dilewati dan dilaporkan.
  - Perbaikan Tipe Data: Menambahkan casting `int()` secara eksplisit pada perhitungan koordinat Stage 2 untuk mencegah error runtime `TypeError: slice indices must be integers`.
   - Hasil: Pemrosesan ulang 168 foto sukses 100% dengan status OK tanpa ada file yang dilewati, dan masalah kebocoran tanggal lama pada ZP 22B, ZP 21A, ZP 12A, dan JL 92 berhasil diselesaikan dengan visual sangat bersih.

- **Perubahan Stage 1: Tanggal Konsensus Folder → Tanggal Lokal Mandiri:**
  - Temuan: File `L60 0.jpg` memiliki tanggal di Y=203, sedangkan `50.jpg` dan `100.jpg` di Y=188. Voting folder memaksa `0.jpg` ke Y=188 → tanggal lama menyembul di bawah (miss posisi).
  - Keputusan User: Stage 1 tidak perlu voting folder karena sudah ada Stage 2 (Alamat Konsensus) sebagai fallback. Stage 1 diubah menjadi deteksi Tanggal Lokal mandiri per file.
  - Perubahan:
    1. `locate_date_box` sekarang memanggil `detect_date_y_center(arr, h, w)` langsung di Stage 1 (tanpa parameter `folder_date_consensus`).
    2. Pre-scan folder tidak lagi mengumpulkan/memproses data tanggal — hanya alamat untuk konsensus.
    3. Parameter `folder_date_consensus` dihapus dari `locate_date_box`, `process_image`, dan `main`.
  - Hasil: `L60 0.jpg` sukses diproses dengan textbox tepat di Y=203 (mengikuti posisi fisik asli).
